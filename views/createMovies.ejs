<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>

  <body>
    <h1>AGREGAR una PELICULA</h1>
    <form action="/movies/createMovies" method="POST">
      <label for="title">Titulo</label>
      <input type="text" name="title" id="title" value="<%= movie.title %>" />
      <label for="rating">Rating</label>
      <input
        type="numbers"
        name="rating"
        id="rating"
        value="<%= movie.rating %>"
      />
      <label for="awards">Premios</label>
      <input type="aw" name="awards" id="awards" value="<%= movie.awards %>" />
      <label for="release_date">Fecha de estreno</label>
      <input
        type="release"
        name="release_date"
        id="release_date"
        value="<%= movie.release_date %>"
      />
      <label for="length">Duracion</label>
      <input
        type="lens"
        name="length"
        id="length"
        value="<%= movie.length %>"
      />
      <label for="genre_id">Genero</label>
      <input
        type="genres"
        name="genre_id"
        id="genre_id"
        value="<%= movie.genre_id %>"
      />
      <button type="submit">Guardar</button>
      <button type="reset">Cancelar</button>
    </form>
    <button><a href="/">Volver al listado de peliculas</a></button>
  </body>
</html>
